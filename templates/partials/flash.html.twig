

    {% for label, messages in app.flashes(['success-salon-delete', 'success-salon-create', 'success-salon-edit', 'success-proposal-edit']) %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
