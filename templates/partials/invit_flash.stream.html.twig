{% for type, messages in app.flashes %}

    <turbo-stream action="append" target="flash">
        <template>
            <div class="alert alert-{{ type }}">
                {{ messages | join('. ') }}
            </div>

        </template>
    </turbo-stream>
{% endfor %}