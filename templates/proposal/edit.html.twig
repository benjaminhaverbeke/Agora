{% extends 'base.html.twig' %}

{% block title %}Proposition{% endblock %}

{% block body %}
    <div class="box proposal-edit">

            {{ form(form, {action: path('proposal.edit', {id: proposal.id})}) }}
        <a class="btn" href="{{ path('salon.index', {id: salon.id}) }}">Retour</a>
    </div>



    {% block chat %}
        {{ include('chat/chat.html.twig') }}
    {% endblock %}
{% endblock %}
